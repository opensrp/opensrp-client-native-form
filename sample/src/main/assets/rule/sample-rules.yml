---
name: step1_hepb_immun_status
description: hepb immunization status
priority: 1
condition: "step1_First_Name == 'John' || step1_First_Name == 'Stacy'"
actions:
  - "isRelevant = true"
---
name: step1_tobacco_cessation_toaster
description: cease tobacco toaster
priority: 1
condition: "step1_hepb_immun_status == 'Seasonal flu dose missing' || step1_hepb_immun_status != 'Joe'"
actions:
  - "isRelevant = true"
---
name: step2_first_name_yo
description: first name
priority: 1
condition: "step1_hepb_immun_status == 'Stacy' && global_has_cat_scan == 'true'"
actions:
  - "isRelevant = true"
---
name: step2_last_name_yo
description: last name
priority: 1
condition: "step2_first_name_yo == 'Tim' && (step2_super_heroes.contains('batman') || step2_super_heroes.contains('the_flash')) "
actions:
  - "isRelevant = true"
---
name: step1_happiness_level
description: Happiness level calculation
priority: 1
condition: "step1_First_Name == 'John' && (step1_hepb_immun_status == 'Stacy' && global_has_cat_scan == 'true') "
actions:
  - "calculation = 1"