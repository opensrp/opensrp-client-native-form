<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/generic_dialog_main_layout"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="50dp">

        <Button
            android:id="@+id/dialog_cancel_button"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_toStartOf="@id/dialog_done_button"
            android:layout_toLeftOf="@id/dialog_done_button"
            android:text="@string/cancel"
            android:background="@null"
            android:textColor="@color/dialog_button_blue_color" />

        <Button
            android:id="@+id/dialog_done_button"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:text="@string/done"
            android:background="@null"
            android:textColor="@color/dialog_button_blue_color" />
    </RelativeLayout>
</LinearLayout>